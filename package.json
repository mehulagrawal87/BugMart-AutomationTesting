{
  "name": "bugmart",
  "version": "1.0.0",
  "description": "BugMart - Manual Testing Playground",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "start": "node server/server.js",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "concurrently": "^8.2.2"
  }
}
